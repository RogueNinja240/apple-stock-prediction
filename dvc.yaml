stages:
  news_scrapping:
    cmd: python src/data/scrape_news.py
    deps:
      - src/data/scrape_news.py
    params:
      - pipeline.ticker
      - pipeline.company
      - pipeline.start_date
      - pipeline.end_date
    outs:
      - data/raw/${pipeline.ticker}_news_data.csv:
          persist: true
  
  data_cleaning:
    cmd: python src/data/preprocess.py
    deps:
      - src/data/preprocess.py
      - data/raw/${pipeline.ticker}_news_data.csv
    outs:
      - data/processed/${pipeline.ticker}_cleaned_news.csv